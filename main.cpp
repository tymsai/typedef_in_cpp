#include<iostream>
using namespace std;
typedef int newname;
int main(){
    newname a;
    cout<<"This program shows the uses of typedef keyword\n";
    cin>>a;
    cout<<"Here the data type name of variable, is changed to a different name , and uses it to identify the data type of the variable \n";
    cout<<a;
    return 0;
}
